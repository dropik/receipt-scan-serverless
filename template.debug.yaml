Globals:
  Function:
    Timeout: 100000

Resources:
  LApi:
    Properties:
      Environment:
        Variables:
          IMAGES_BUCKET: receipt-scan-dev-images-bucket

  LScanner:
    Properties:
      Environment:
        Variables:
          DB_CONNECTION_STRING:
