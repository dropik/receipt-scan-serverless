Globals:
  Function:
    Timeout: 100000

Resources:
  FileUploader:
    Properties:
      Environment:
        Variables:
          IMAGES_BUCKET: receipt-scan-dev-images-bucket

  ReceiptRecognitionService:
    Properties:
      Environment:
        Variables:
          DB_CONNECTION_STRING:

